import java.applet.*;
import java.awt.*; 

class xyz extends Applet {
    Button b1;

    setLayout(new BorderLayout());
    add(b1.BorderLayout.NORTH);
    add(L1.BorderLayout.WEST);
}

// idk whats happenin